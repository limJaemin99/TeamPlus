<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.teamplus.mvc.dao.MyNoteMapper">

    <!-- 노트 번호 시퀀스 -->
    <select id="getSequence" resultType="int">
        SELECT CASE WHEN MAX(noteNo) is null THEN 1
                    ELSE MAX(noteNo)
                   END AS noteNo
        FROM MYNOTE
    </select>

    <!-- 노트 글 작성 -->
    <insert id="write" parameterType="MyNoteDTO">
        INSERT INTO
            myNote
        VALUES (#{userNo},#{noteNo},#{noteDate},#{title},#{content})
    </insert>

    <!-- 사용자 + 날짜 지정 노트 글 리스트 [출력] -->
    <select id="selectList" parameterType="java.util.Map" resultType="MyNoteDTO">
        SELECT *
        FROM myNote
        WHERE userNo=#{userNo} AND noteDate=#{noteDate}
    </select>

    <!-- 사용자 + 날짜 지정 노트 글 [삭제] -->
    <delete id="delete" parameterType="String">
        DELETE FROM
            myNote
        WHERE
            noteNo=#{noteNo}
    </delete>

    <!-- 사용자 + 날짜 지정 노트 글 [수정] -->
    <update id="update" parameterType="MyNoteDTO">
        UPDATE myNote
        SET title=#{title},content=#{content}
        WHERE noteNo=#{noteNo}
    </update>

</mapper>